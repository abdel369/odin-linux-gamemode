#!/usr/bin/env bash
set -euo pipefail

export SDL_VIDEODRIVER="${SDL_VIDEODRIVER:-wayland}"
export QT_QPA_PLATFORM="${QT_QPA_PLATFORM:-wayland;xcb}"
export STEAM_FRAME_FORCE_CLOSE="${STEAM_FRAME_FORCE_CLOSE:-1}"

exec steam -gamepadui "$@"
